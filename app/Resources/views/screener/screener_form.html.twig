{{ form_start(form, {'attr': {'id': 'screener-filters-form'}}) }}
                    {% form_theme form 'Form/screenerFields.html.twig' %}
                    {{ form_widget(form) }}
                    {{ form_end(form) }}
    <script>


    </script>
